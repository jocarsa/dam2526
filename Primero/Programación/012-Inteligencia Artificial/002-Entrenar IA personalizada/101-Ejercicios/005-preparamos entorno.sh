Me meto dentro de la carpeta del proyecto

Creamos un entorno virtual
python3 -m venv venv

Activamos el entorno virtual
source venv/bin/activate

Instalamos dependencias

pip install torch
pip install datasets
pip install peft
pip install transformers
