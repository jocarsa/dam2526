{
  "faq": {"title":"Preguntas Frecuentes","qas":[{"q":"¿Cómo se conecta a SQLite y crea tablas si no existen?","a":"La función `getDBConnection()` establece la conexión a la base de datos SQLite. Si el archivo de base de datos no existe, las tablas 'users' y 'calendars' son creadas. Además, un usuario inicial (admin) se registra con contraseñas hasheadas."},{"q":"¿Cómo obtiene un usuario por su nombre de usuario?","a":"La función `getUserByUsername()` busca en la base de datos a un usuario que coincida con el nombre de usuario proporcionado y devuelve sus detalles si existe."},{"q":"¿Cómo se registra un nuevo usuario?","a":"La función `registerUser()` verifica si el nombre de usuario ya está registrado. Si no, crea un nuevo registro para el usuario proporcionado con su contraseña hasheada."},{"q":"¿Cómo se inicia una sesión de usuario?","a":"La función `loginUser()` verifica la existencia y validadidad del usuario a través de la base de datos utilizando `password_verify()`. Si todo es correcto, inicia la sesión almacenando el ID del usuario en `$_SESSION`."},{"q":"¿Cómo se verifica si una sesión está iniciada?","a":"La función `isLoggedIn()` simplemente verifica si existe la variable `$_SESSION['user_id']`, indicando que la sesión de un usuario está activa."},{"q":"¿Cómo se cierra una sesión de usuario?","a":"La función `logoutUser()` elimina todos los datos de la sesión con `session_unset()` y luego destruye la sesión con `session_destroy()`, cerrando así la sesión del usuario."}]
}